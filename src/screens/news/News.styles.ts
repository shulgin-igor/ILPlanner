import styled from 'styled-components/native';
import {cornFlower, veryLightPink} from '../../ui/Colors';

export const Title = styled.Text`
  font-size: 16px;
  line-height: 24px;
  font-weight: 500;
  color: ${cornFlower};
`;

export const Date = styled.Text`
  font-size: 14px;
  line-height: 18px;
  color: ${veryLightPink};
`;
