import styled from 'styled-components/native';
import {slateBlue} from '../../../../../ui/Colors';

export const Container = styled.View``;

export const Title = styled.Text`
  text-align: center;
  font-size: 32px;
  font-weight: 700;
  line-height: 32px;
  color: ${slateBlue};
  margin: 10px 0;
`;
